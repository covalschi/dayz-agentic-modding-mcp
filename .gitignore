__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
.dayz-mcp/
dayz-mcp.local.toml

# bridge/ is source; its packed output (pbo, signature, borrowed public key)
# is a build artifact like any other project's @ModName, reproduced by
# packer.pack_one from bridge/ -- never committed.
/@DZMCP_Bridge/

# local Claude Code permissions, not part of the product
.claude/

# the project installs with pip into .venv; uv.lock is left behind by a
# stray `uv` run, not a file this project's own tooling produces or reads
uv.lock
